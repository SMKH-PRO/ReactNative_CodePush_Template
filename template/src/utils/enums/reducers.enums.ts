enum UserReducer {
  SET_USER = 'SET_USER',
}

enum ThemeReducer {
  DARK_MODE = 'DARK_MODE',
  SET_THEME = 'SET_THEME',
}

enum AppConfigReducer {
  SET_LANG = 'SET_LANG',
}

export { UserReducer, ThemeReducer, AppConfigReducer };
